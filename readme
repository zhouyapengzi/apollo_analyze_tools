*Tools to help analyze apollo*

count_file_change_times.py : count how many times the file is changed in the past.

get_tests_assertion.py : get test cases and each assertions from a test file

analyze_proto/output/proto_analyze : protos in apollo are decoded to .pb.cc and .pb.h 

build_proto_dependency_graph.py : help to build dependency graph

neuron_coverage: mlp neuron coverage is done based on Apollo-5.0. when using it, copy the file to replace apollo files. the neuron coverage will be printed in apollo logs. caffe for perception only partly done. 

insertLogToMethod: script to insert logs at the beginning and end of a method.

 
